<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
    </plugin-info>
    
    <jql-function key="current-project" name="Current Project Function" class="com.acronis.jira.CurrentProjectFunction">
		<fname>currentProject</fname>
		<description>Provides a JQL function to return the current project.</description>
		<list>false</list>
	</jql-function>
    
</atlassian-plugin>
